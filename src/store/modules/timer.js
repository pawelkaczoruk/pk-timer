const state = {
  selectedCube: 'c3'
};

const getters = {
  getCube: state => state.selectedCube
};

const actions = {

};

const mutations = {

};

export default {
  state,
  getters,
  actions,
  mutations
}